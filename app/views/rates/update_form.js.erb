// clear error messages on form
$('#new_rate #errorExplanation').remove();

// update form with error messages
$('#new_rate').prepend("<%= escape_javascript(error_messages_for(@rate)) %>");
$('#new_rate').show();
